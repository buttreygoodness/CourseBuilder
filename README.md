CourseBuilder
=============

Install meteor
--------------

`$ npm install -g meteor`

Install meteorite
-----------------

`$ npm install -g meteorite`

Install the meteorite packages
------------------------------

`$ mrt install`

Run the application
-------------------

`$ mrt`

